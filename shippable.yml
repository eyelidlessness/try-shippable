language: clojure

lein:
    - lein2

before_script:
    - mkdir -p shippable/testresults

script:
    - lein spec -f junit > shippable/testresults/junit.xml

branches:
    only:
        - master
        - develop
